<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal"> <!-- Center the entire content horizontally -->

    <!-- Image Preview Section -->
    <ImageView
        android:id="@+id/productImagePreview"
        android:layout_width="282dp"
        android:layout_height="301dp"
        android:layout_gravity="center"
        android:contentDescription="Product Image Preview"
        android:scaleType="centerCrop"
        android:src="@drawable/dummy_product" /> <!-- Centers the image horizontally -->

    <!-- Button to Upload Image -->
    <Button
        android:id="@+id/uploadImageButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#BBD4F6"
        android:gravity="center"
        android:text="Upload Image"
        android:textColor="@android:color/black"
        android:textSize="12sp"
        android:textStyle="bold"
        android:layout_gravity="center" />

    <!-- Product Name Input Field -->
    <EditText
        android:id="@+id/editProductName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Product Name"
        android:inputType="text"
        android:layout_marginBottom="16dp" />

    <!-- Product Price Input Field -->
    <EditText
        android:id="@+id/editProductPrice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Price"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp" />

    <!-- Product Description Input Field -->
    <EditText
        android:id="@+id/editProductDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Product Description"
        android:inputType="textMultiLine"
        android:lines="3"
        android:layout_marginBottom="16dp" />

    <!-- Submit Button -->
    <Button
        android:id="@+id/saveProductButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="#4CAF50"
        android:gravity="center"
        android:text="Save"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:layout_gravity="center" />

</LinearLayout>
